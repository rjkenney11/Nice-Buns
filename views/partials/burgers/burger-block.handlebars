<li>
	{{burger_name}}
	<br>
		{{#if devoured}}<button class="change-yum" data-id="{{id}}" data-yum="{{devoured}}">
	DEVOURED</button> {{/if}}

	<button class="delete-yum" data-id="{{this.id}}">DELETE!</button>
</li>
